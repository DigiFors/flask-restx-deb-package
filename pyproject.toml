[project]
name = "flask-restx-deb-package"
version = "1.3.2"
description = "Deb package builder for flask-restx"
requires-python = ">=3.10"
dependencies = [
]

[project.optional-dependencies]
dev = [
    "stdeb"
]
